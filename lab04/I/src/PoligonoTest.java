import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

public class PoligonoTest {

    // Teste para polígono inválido (apenas 2 pontos)
    //@Test
    /*public void testInvalidPolygon() {
        Ponto[] pontos = {new Ponto(1,1), new Ponto(2,2)};
        Poligono p = new Poligono(pontos);
        assertEquals("Poligono:vi", p.toString());
    }*/

    // Teste para polígono válido (quadrilátero)
    //@Test
    /*public void testValidPolygon() {
        Ponto[] pontos = {
                new Ponto(0,0),
                new Ponto(4,0),
                new Ponto(4,3),
                new Ponto(0,3)
        };
        Poligono p = new Poligono(pontos);
        assertEquals("[(0,0), (4,0), (4,3), (0,3)]", p.toString());
    }*/
}